import { LoadingState } from "@/components/loading-state";

export default function AppLoading() {
  return (
    <LoadingState
      description="Preparing your projects and tasks."
      title="Loading workspace"
    />
  );
}
